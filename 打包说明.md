# 我的课表 · 手机安装说明

这是一个 **PWA（渐进式网页应用）**，可以把「课程表 + 考试日历」安装到手机桌面，像原生 App 一样全屏运行，支持离线使用。

## 一、文件清单

| 文件 / 目录 | 作用 |
|---|---|
| `课表.html` | 主程序（单文件，含课程表 + 考试日历 + 深色模式） |
| `index.html` | 入口跳转页（部署后访问目录根路径用） |
| `manifest.json` | PWA 清单（应用名称、图标、启动方式） |
| `sw.js` | Service Worker（离线缓存） |
| `icons/` | 应用图标（192 / 512 / 180 / maskable） |

## 二、必须通过 HTTPS 部署（重要）

PWA 的「添加到主屏幕」「离线缓存」需要 **HTTPS**（或 `localhost`），直接双击 `课表.html`（file://）无法触发安装。

免费部署方式任选其一：

1. **GitHub Pages**：把整个文件夹推到 GitHub 仓库，仓库设置 → Pages 开启，得到 `https://你的用户名.github.io/仓库名/`。
2. **Netlify / Vercel**：把文件夹拖进去即可，自动 HTTPS。
3. **本地测试**：在文件夹里运行 `python -m http.server 8123`，电脑浏览器访问 `http://localhost:8123/`（仅本机，手机装不了）。

## 三、安装到手机

**安卓（Chrome 或 Edge）**
1. 手机浏览器打开部署后的网址。
2. 右上角菜单 → 「安装应用 / 添加到主屏幕」→ 确认。
3. 桌面出现「我的课表」图标，点开即全屏运行。

**iPhone（Safari）**
1. Safari 打开部署后的网址。
2. 点底部「分享」按钮 → 上滑 → 「添加到主屏幕」。
3. 桌面出现「我的课表」图标。

## 四、更换应用图标

当前 `icons/` 里是自动生成的**占位图标**（渐变底 + 课程表格纹样）。

请提供一张 **正方形、≥512×512** 的 PNG/JPG 图（透明底或纯色底均可），放到本文件夹（例如 `icon.png`），即可生成正式图标替换 `icons/` 里的四个文件。

## 五、数据说明

- 课程、考试、主题、学期日期都保存在**浏览器本地**（localStorage）。
- 数据按「网站域名」隔离：换一个部署地址（如从 localhost 换成 GitHub Pages），数据是分开的，不会自动迁移。
- 若要换域名，可先在旧地址点「⇩ 导出」，再到新地址用对应方式导入。
