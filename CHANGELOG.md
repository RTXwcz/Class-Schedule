# 更新记录

## 1.7.0 — 2026-09-11

按对抗式审查的发现逐条修复：

- 复核页显示“已追加 N 门课程，M 门待补充”，部分导入不再只看到报错。
- 全览档冲突课程改为“点卡片先选一门再编辑”，被合并的课程不再没有入口。
- 云端识别契约改为 `{courses, periods}`，云端也能带回一天作息并应用为每日作息。
- MCP 服务增加每对端 10 秒 60 次限流与 1 MiB 请求上限，设置页加入公共 Wi-Fi 明文 Token 提示。
- release 构建缺少签名环境时直接失败（CI 用 `CLASS_SCHEDULE_ALLOW_UNSIGNED=1` 做编译校验），CI 同时构建 release 变体覆盖 R8/资源收缩。
- 备份导入：拒绝更高 schemaVersion、支持字符串周次（“1-8,10”）、校验考试与调休日期。
- MCP Token 解密失败（截断、AEAD、密钥丢失）统一轮换，不再把异常抛进鉴权链路。
- 冲突标记按“到目前最大结束节次”判断，链式重叠不再漏标。
- 作息识别容忍节次跳号；教师姓“楼”不再被当成教学楼，折行标题不再把续行当教师。
- 新增 Room 迁移测试（3→4）、导入持久化、冲突选择与限流回归测试。
- 单元测试 109 项、Android 15 设备测试 70 项全部通过。

## 1.6.4 — 2026-09-11

- 修复“识别出十几门课，导入后只进去一门”的问题：导入以前是全有或全无，只要有一门课超出当前作息就会整批回滚。现在可用的课程照常入库，超出的那门留在待补充并说明原因。
- 若提示是节次超出当前作息，而图片里又识别到了作息（时间轴），提示会直接告诉你先点“应用为每日作息”；照做后剩余课程即可继续导入。
- 节次无法从结构判断时不再留空（留空会让整批无法应用），改为按“至少占用起始那一节”填入并标为低置信，复核页里仍可修。
- 新增真机（Room）回归测试：可导入的课程落库、超出的课程保留在复核列表、补齐作息后剩余课程成功入库。
- 单元测试 103 项、Android 15 设备测试 67 项全部通过。

## 1.6.3 — 2026-09-11

- 云端识别可用性修复：API 地址只填服务根地址也能用（自动补全 `/v1/chat/completions`），请求显式关闭流式返回，服务端的错误原文与状态码一起显示，设置页新增「测试连接」，并写明必须选支持图片输入的模型。
- 实测可用组合：DeepSeek `https://api.deepseek.com` + `deepseek-flash`；中转站 `https://direct.abinapi.com` + `Qwen3.8-Next` 系列需用 `/v1` 端点。把控制台首页当 API 地址时（返回 HTML）现在会明确提示，而不是报“响应缺少课程内容”。
- 地点信息不再丢括号：`紫金港田径场（东）`、`紫金港风雨操场（羽毛球场）` 原样保留；`紫金港东2-204` 仍会拆成教学楼 + 房间号。
- 新增“从课表读作息”：解析图片时间轴，得到一天多少节课以及每节课的起止时间，复核页可一键「应用为每日作息」，提醒与小组件随新作息生效。
- 修正测试对日期的硬编码，避免跨天后“期末数学”被当成过去事项。
- 单元测试 102 项、Android 15 设备测试 66 项全部通过。

## 1.6.2 — 2026-09-11

- 重写图片识别的**表格结构利用**：同一单元格内的文本再也不会被拆成多门课（此前教务系统导出的截图会产出 98 条垃圾，现在同一张图得到 14 条正确课程）。
- 表头“星期一”被识别成“星期”时按位置补回缺失的星期，不再让周一的课落到周二。
- 识别“单/双”子列标题：单周／双周直接写入课程规则，子列分割线丢失时按两个标题的中点判断。
- 字段归类：课程名（含折行标题）、教师（含“张三/李四”多人写法）、教学楼与教室（“东2-204”拆成楼名 + 房间号）、周次全部填进对应字段；日期时间戳与“N节/周”属表格元信息，按“能填则填、不能填则丢弃”删除。
- 修正把“第1-8周2节/周”的“2节”误读为节次的严重错误；同一节课按节重复的行会合成一条并给出正确跨度。
- 时间轴列（节次号、时钟、早晨/上午/下午/晚上）不再被识别成课程。
- 新增「教务导出截图」形态的解析回归测试；单元测试 100 项、Android 15 设备测试 66 项全部通过。

## 1.6.1 — 2026-09-11

- 修正 1.6.0 把最窄档按“并行列总数”反算列宽的严重问题：并行课程多的课表会被压成 20 多 dp 的细条，日期折成两行、教学楼与教室被挤没。
- 最窄档恢复为**每天固定一列**、列宽按七天平分（411dp 屏约 49dp），日期与“教学楼 + 教室”始终完整显示。
- 同一天时间冲突的课程不再并排挤宽整周，而是在该天的列内合成一张卡片，逐条列出课名与地点，用细分隔线分开；标准/宽松档仍按并列分栏显示，方便对比。
- 星期表头拆成“周几”“日期”两个单行标签并禁止换行，日期不会再被拆成“09/0 / 7”。
- 修正切档时课表布局被缓存导致合并结果不刷新的问题（布局缓存键加入当前档位）。
- Android 15（411dp）与 Android 7.0（360dp）各 61 项设备测试通过。

## 1.6.0 — 2026-09-11

- 同一天里连续节次、同一门课（课名、地点、教师、周次、单双周全部一致）的记录自动合并成一张卡片，OCR 或手工录入把连堂课拆成多行时不再出现一叠碎卡片。
- 合并只是显示层：课程数据、编辑弹窗和“全部课程”列表仍保持原来的单节记录，编辑合并卡片时会打开其中的第一节，不会把合并后的跨度写回数据库。
- 最窄档的列宽改为按真实列数反算，星期内有并行课程时自动收窄，保证七天始终整体显示在屏幕内，不会再被挤到屏幕外。
- Android 15（411dp）与 Android 7.0（360dp）各 60 项设备测试通过。

## 1.5.5 — 2026-09-11

- 课表拖动改为两轴自由平移：一次滑动同时改变日期和节次，不再被锁定在单一方向。
- 松手后按手势速度继续惯性滑行，新一次触摸立即停住；双指捏合缩放的同时也能直接拖动定位。
- 惯性由时间积分实现，不依赖帧时钟，在不同刷新率与低端设备上滑行距离一致。
- Android 15（411dp）与 Android 7.0（360dp）各 57 项设备测试通过，新增“一次拖动同时移动两轴”与“快速滑动比慢速拖动滑得更远”两条断言。

## 1.5.4 — 2026-09-10

- 课表提供两种浏览方式：默认“全览”按屏幕宽度自动排布，七天同屏；“标准”(92dp) 与“宽松”(130dp) 为固定列宽，左右拖动浏览不同天。
- 支持双指捏合连续缩放 40–220dp，捏到比全览更窄时吸附回全览；右上角按钮显示当前档位，点按循环切换并保留横向浏览位置。
- 全览档课程卡片不再重复“第 X–Y 节”，单双周标记保留；行高需求计入卡片内缩与行间距，窄屏长课名在 Android 7 的折行规则下也不会被裁。
- 表头改为“周几 + 日期”两行并在窄列中居中。
- Android 15（411dp）与 Android 7.0（360dp）各 55 项设备测试全部通过。

## 1.5.3 — 2026-09-10

- 节次高度改为按内容分配：只有被课程覆盖的节次变高，空节次保持紧凑，同一天与跨天仍严格对齐。
- 顶部工具区压缩为三行（标题与操作、周导航、当前课程单行条），把垂直空间交还课表。
- 底栏改为居中的窄条，宽度约为屏幕的 92%、上限 420dp，每个标签获得等宽槽位，图标与文字不再被裁切。
- “紧凑”档列宽缩到标准档的 70%，课名折行显示；课程卡片只显示实际存在的信息。
- 桌面小组件内容与圆角边框之间增加左右和上下内边距，五种尺寸与两种主题渲染仍全部通过。

## 1.5.2 — 2026-09-09

- 进一步压缩课表空白：标准行高从 68dp 收紧到 56dp，长标题仍按内容自动增加空间。
- “紧凑”档改为约 70% 列宽，课程名称折行显示；底部导航栏缩短并缩小图标/文字。
- 课表上方采用更紧凑的标题、当前课程卡和周导航布局，保留固定列宽、双向滚动与刻度对齐。
- Android 7/15、横屏、130%字体与固定课表回归更新；验证见 `docs/product/1.5.2/VERIFICATION.md`。

## 1.5.1 — 2026-09-08

- 周课表统一为固定列宽与字号，取消七天压缩及展开/整周切换；重叠课程保持独立宽度。
- 支持横向日期与纵向节次滚动，顶部星期和左侧时间刻度固定同步；长课名与地点完整换行。
- 所有节次保持统一高度，连堂跨格与各天同节对齐；支持系统大字体所需空间。
- 添加课程入口移到操作栏，避免遮住最后一节；日期跳转定位目标星期。
- Android 7/15、横屏及130%字体验证见 `docs/product/1.5.1/VERIFICATION.md`。

## 1.5.0 — 2026-09-08

- 新增鸢尾紫配色，支持浅色、深色及跟随系统；桌面组件同步配色。
- OCR 校对改为默认折叠摘要、展开编辑、底部一次应用，取消逐字段勾选。
- 增加缺项定位、仅修改所选内容的统一周次、可跨返回保留的移除撤销；防止重复/过期保存回调再次写入。
- 切换页面保留模式、筛选与位置，近期安排优先，历史记录可展开。
- 时段滚轮支持真正取消并等待滚动停止；全天切换保留时间；全部课程编辑后返回原位置；手动草稿隐式退出保护。
- 两种 ARM 包、GPL-3.0-only、模型按需下载；验证证据见 `docs/product/1.5.0/VERIFICATION.md`。

## 1.4.2 — 2026-09-08

- 修复空组件点击录入、后台与重复点击导航，补齐 Android 15/16 的 Widget Activity 启动选项。
- 五种组件尺寸统一首课重点与后两课层级，修复实际字体行高造成的裁切。
- OCR 校对增加顶部返回与独立放弃，保留会话内草稿；禁止新识别覆盖未处理草稿。
- 两种本地模型改进不等宽列、闭合格线跨度、折行标题和低置信字符复核，防止缺列误归属、独立短课程误合并与跨行候选复用。
- 移除 Capacitor/Cordova 运行时和旧 Web 副本，启用 R8 与资源收缩；ARM64 APK 从 60.72 MiB 降到 45.13 MiB，ARMv7 从 48.17 MiB 降到 32.57 MiB。
- 验证范围、截图与已知边界见 `docs/product/1.4.2/VERIFICATION.md`。正式附件仍仅有两种 ARM 包。

## 1.4.1 — 2026-09-08

- 桌面小组件采用圆角极简设计：大号下一节时间、日期标记、后两节摘要与空状态。
- 提供紧凑/标准/较大尺寸的适配布局，随系统昼夜主题切换。
- 深色模式改为石墨黑与中性灰层次，调整重点色、课程配色和下一节面板亮度。
- 保留 ARM64/ARMv7 独立签名包；视觉与渲染证据见 `docs/product/1.4.1/VERIFICATION.md`。

## 1.4.0 — 2026-09-08

### 使用体验

- 重新设计原生视觉体系：深绿与暖白、独立课程配色、下一节课提示、横屏紧凑工具栏和固定时间刻度。
- 学期、调休、考试/日程与作息使用日期/时间滚轮，处理闰年和月份变化；上课时段也可滚动选择。
- 增加“全部课程”管理入口，暂未生效的课程可以随时编辑和删除。
- MCP 设置提供三步连接引导、地址/Token/完整 JSON 复制、确认模式和连接排查；高级端口设置折叠。
- 改善初次使用的手动录入和图片识别引导，补充删除确认和调休编辑。

### 安全与正确性

- 修复 Web 导入考试/日程 ID 可触发的存储型 HTML 注入，更新 PWA 离线缓存。
- 课程、学期、周次和作息改为同一 Room 事务提交，避免恢复备份时发生部分写入；数据库升级为 v4。
- 修复跨午夜提前提醒丢失和跨日去重问题。
- 修复 MCP 清空确认期间覆盖新规则的问题，并统一读取权威数据集。
- 使用 AndroidX ExifInterface 1.4.2 处理图片方向，增加旋转 JPEG 回归。

仅发布 ARM64 和 ARMv7 APK，沿用原正式签名。详细审查与验证见 `docs/reviews/README.md`。

## 1.3.0 ARM 打包调整 — 2026-09-08

- 仅保留 arm64-v8a（ARMv8）和 armeabi-v7a（ARMv7），各提供一个独立签名 APK。
- 移除包含 x86/x86_64 的通用 APK 下载，功能和发布签名保持一致。
- Release 附件中的 ARM 源码包包含更新后的 ABI 打包配置；原始 v1.3.0 标签保留为首次发布记录。

## 1.3.0 — 2026-09-07

首个 GitHub Release。版本号沿用项目内部的 1.3 系列。

- 原生 Kotlin/Compose 周课表、课程与考试编辑、主题设置。
- 默认整周概览、展开课表、今日强调和课程统计；录入页提供手动课程/考试/日程入口。
- 单双周成为可选开关；关闭时保留原规则，并统一影响界面、通知、Widget 和 MCP。
- 新增日程类型、筛选和普通备注，兼容旧考试；Room v2→v3 迁移保留已有记录。
- OCR 模型默认从 `hf-mirror.com` 获取，仍使用固定版本、文件大小及 SHA-256 校验。
- 自定义每天 1–48 节、每节起止时间；课表、提醒、Widget、MCP 和 JSON 同步使用作息。多节连堂课程显示为单张连续卡片。
- 单双周、明确周次集合、整天调休，以及教学楼/教室/备注分栏。
- 默认提前 10 分钟提醒、提醒恢复与去重、最近三节课桌面小组件。
- 按需下载 PP-OCRv6 tiny/small 本地 OCR；OpenAI 兼容识图；导入字段逐项确认。
- Room 数据迁移、稳定数据集元数据和完整 JSON 导入导出；Web 自动视图应用周次和调休。
- 局域网 MCP 的 Token 鉴权、可选逐次写入确认和后台前台服务。
- GPL-3.0-only、第三方许可证、应用内开源许可入口和正式发布签名。

### 已知限制

- 验证使用 Android 15 模拟器；真实手机省电策略、重启通知准点率和真实课表准确率仍待测量。
- OpenAI 兼容接口尚未使用真实 Key 做付费调用测试。
- 作息使用同一天的 24 小时制时间，不支持单节跨午夜。
- Web 原生字段的编辑入口有限；Web 的手动周次筛选保留模板查看行为。
- 首次正式签名与历史 debug 签名不同，切换前需导出 JSON 备份。
